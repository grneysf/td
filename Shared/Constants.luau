--!strict

const SIM_HZ = 60
const NET_HZ = 20
const SIM_DT = 1 / SIM_HZ
const NET_DT = 1 / NET_HZ

const MAX_SPAWN_BATCH = 64
const MAX_STATE_BATCH = 128
const MAX_SPEED_BATCH = 64
const MAX_DAMAGE_BATCH = 64
const MAX_DEATH_BATCH = 150

const CORRECTION_EPSILON = 0.005
const SPEED_EPSILON = 0.01

local Constants = {
	SIM_HZ = SIM_HZ,
	NET_HZ = NET_HZ,
	SIM_DT = SIM_DT,
	NET_DT = NET_DT,
	MAX_SPAWN_BATCH = MAX_SPAWN_BATCH,
	MAX_STATE_BATCH = MAX_STATE_BATCH,
	MAX_SPEED_BATCH = MAX_SPEED_BATCH,
	MAX_DAMAGE_BATCH = MAX_DAMAGE_BATCH,
	MAX_DEATH_BATCH = MAX_DEATH_BATCH,
	CORRECTION_EPSILON = CORRECTION_EPSILON,
	SPEED_EPSILON = SPEED_EPSILON,
}

return Constants